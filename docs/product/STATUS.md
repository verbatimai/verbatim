# STATUS / HANDOFF — Verbatim

Snapshot so any new session (or teammate) can continue with zero context loss. The **repo is the source of truth**, not any chat.

_Last updated: 30 Aug 2026 — **the O-track Rust COMPILES on the Mac** (`cargo build` clean, 0 errors); the long-standing `glossary_get`/`glossary_save` `invoke_handler` gap is **fixed** (53 → 55 handlers). TS typecheck-green + **276 core tests passing**. Runtime click-through still pending. Previous entry: 18 Aug 2026 — first-run onboarding (O1–O7) authored end-to-end by a 3-agent pipeline, with all of the Rust then uncompiled. Read §First-run onboarding before touching `apps/widget`. Earlier: 13 Aug 2026 (late) — meetings/N-track prototype landed; see the **Meetings** bullet and §Parallel-session notes._

## Where things stand
- **M0 Foundation** — done, except `git init` (see below).
- **M1 Core pipeline** — done & tested (offline). Live latency (F9) still to measure on a Mac.
- **M2 Live web demo** — working. Output (cleanup + formatting) is clean and correct. Live input streams as a growing transcript. **Robust design:** live input = `TranscriptAccumulator` over Hear's stream; **final output = BATCH transcription of the full audio on stop** (`/v1/audio/transcriptions`) → cleanup → format, so the inserted result never depends on reconstructing the messy stream.
- **M3 Desktop widget** — in progress. **Phase 3.0 spikes ✅ both confirmed on a real Mac (11 Aug 2026) — hard gate cleared.** **Spike A** (non-activating overlay): built & run via `apps/widget`; the widget floats over Notes/Slack without stealing focus — caret keeps blinking and typing still goes to the other app. **Working recipe:** `tauri-nspanel` **`v2.1`** branch, `tauri_panel!` macro with `can_become_key_window: false` + `is_floating_panel: true`, reclass the "main" window via `window.to_panel::<SpikePanel>()`, `set_style_mask(NSWindowStyleMask::NonactivatingPanel)` (typed via the crate's re-exported `objc2_app_kit`), `set_activation_policy(Accessory)`, ⌥Space shows without `set_focus`. The **non-key** part (`can_become_key_window:false`) was the piece that fixed keystrokes being swallowed by the widget. **Spike B** (injection): clipboard + ⌘V `inject_text` pastes into the focused field with no countdown (focus never leaves the target app). Note: needed an app icon at `src-tauri/icons/icon.png` (added) for `generate_context!`. **Phase 3.1 ✅ (real app scaffold):** the widget now runs the **M2 dictation UI** in the overlay (streaming transcript + "what was removed" diff + final-output box) and injects the finalized text into the focused field on Stop. Vanilla-TS ported from `apps/web`; the pipeline + key stay in the M2 backend (WS on `127.0.0.1:8787`), and the one widget-specific seam is `formatted` → Rust `inject_text`. Run with **`npm run widget`** (backend + `tauri dev`). Frontend typechecks clean. **Phase 3.4 ✅ DONE (14 Aug 2026) — reliable AX focus read + routing.** Root cause of the earlier `-25212`: the *system-wide* focused-element attribute is unreliable (worse for Chromium/Electron, whose AX tree is lazy). Fix (`axinject.rs read_focus`): frontmost pid via **NSWorkspace** (minimal objc FFI) → **`AXUIElementCreateApplication(pid)`** → set **`AXManualAccessibility=true`** → **poll `AXFocusedUIElement`** for the lazy tree → system-wide fallback → paste fallback (AX never blocks paste). Routing: not-trusted→copy, **secure field→refuse+copy**, editable→paste, non-editable→copy, unreadable→paste. **AX-write (`kAXSelectedText`) intentionally unused** (silent no-op on tested apps). Added a **`⌥⇧V` paste-test hotkey** (inject a fixed sentence with no backend). **This satisfies the M3 exit requirement (password fields refused).** **Phase 3.3 ~80%:** overlay joins all Spaces + full-screen (collection behaviour); caret-anchored positioning left (but the draggable-orb / remembered-position model is the current, intentional placement). **Phase 3.2 ~80% (hotkey + Wispr-style UX):** ⌥Space drives dictation with **tap-to-toggle** and **hold-to-talk** (Rust `Pressed`/`Released` state machine → `dictation` events the webview acts on). UX reworked to a **floating draggable orb** (idle, remembers position) that opens the **full streaming card** on start (transcript streams live — matches the SOP) and collapses back after inserting; card opens anchored to the orb, clamped on-screen. **Live mic-level meter** (AnalyserNode) in the card; **menu-bar tray icon** as the running indicator. Remaining 3.2: configurable hotkey (needs a settings store), `fn`-hold bare-key (native CGEventTap + Input-Monitoring), and context-gating the orb (now possible via the AX read, but needs a lightweight background poll). **M3 roadmap exit criteria are essentially met** (hotkey → dictate over a 3rd-party app → corrected text inserted → widget never steals focus → password fields refused) — pending a formal on-Mac exit demo. **Phase 3.5 ✅ (keychain BYOK):** keys stored/read/cleared via `keyring` + a Settings screen; because the non-key panel can't accept a typed/pasted key, entry is via **`key_save_clipboard`** (Rust reads the clipboard directly and stores it, returns a masked `••••1234`). Key handed to the backend for the session. **Phase 3.2/3.4/3.6 polish ✅ (13 Aug 2026):** **configurable hotkey** (click-to-pick preset list → `set_toggle_hotkey`, persisted + re-registered live via a `CURRENT_TOGGLE` static); **proactive Accessibility status** in Settings (`ax_trusted`); **re-show last result** (tray "Show Last Result" → `show-last`); **show/hide animations** (card scale-in, settings fade); plus bug fixes from on-Mac testing: the clipboard key-entry above (typing into the panel is impossible by design) and **relabelled live-preview vs final-output** (they come from two engines — rolling on-device preview vs batch cloud final — so divergence + a dropped trailing word are expected, not failures). **M3 roadmap exit criteria are met; only a formal on-Mac exit demo remains to sign it off.** **Deferred to M4 (with rationale in `m3-tasks.md`):** `fn`-key hold (native `CGEventTap` + Input-Monitoring — own spike), orb context-gating (continuous background AX poll), caret-anchored positioning (superseded by the draggable orb), and going backend-free (client-side `@verbatim/core` → drop the dev backend; the M4 backbone).
- **M4 Desktop app + multi-vendor + config** — ✅ **FUNCTIONALLY COMPLETE (14 Aug 2026).** The release `Verbatim.app` builds on the Mac with the backend **sidecar bundled** (`Contents/MacOS/verbatim-backend`), the app owns/keys the sidecar (no key in the renderer), and **full live dictation works end-to-end on OpenAI/Anthropic/Deepgram** (streaming STT → visible correction → injection). Only follow-ups remain: exhaustive per-combo + non-English confirmation, and **PyAI is externally 404-ing** (its `/v1/messages` correction + Hear stream — vendor-side, tracked with PyAI; **rotate the leaked PyAI test key before M6**). Detail: **Done:** 4.0 decisions, 4.1 core config/capability, **4.2 settings-window split**, **4.3 config store + keychain**, **4.7 settings UI + multilingual** (Rust; `cargo build`/`cargo build --release` clean on the Mac, release `.app` launched standalone with no panic; on-Mac *interactive* click-through for 4.2/4.3/4.7 still pending), and **all four vendor adapters** — **Deepgram STT (4.4)**, **OpenAI STT+correction (4.5)**, **Anthropic correction (4.6)** (cloud-tested vs mocks; Deepgram is STT-only by design). 4.7 also added the core-level language localization (non-English note in `prompt.ts`, threaded through all 3 correction adapters + `Pipeline`) and a generic hotkey-accelerator parser (`main.rs`) so Settings can capture **any** key combo, not just the 5 old presets. **4.8 sidecar wiring — dev code done** (Rust `spawn/kill/restart_backend` injects all Keychain keys into the backend env, `msg.apiKey` removed from `server.ts`, overlay `connect()` sends only provider/language, `widget.mjs` no longer launches the backend) — needs `cargo build`/`npm run widget` verify. **Release packaging landed (`885b68d`):** `externalBin` sidecar + `beforeBuildCommand` compile (`scripts/build-sidecar.mjs`, bun), release spawn branch in `main.rs`, `entitlements.plist` (mic + JIT), and `bundle.macOS` config; sign/notarize steps in `docs/architecture/release-signing.md` — remaining is the on-Mac `tauri build` + code-sign run. **4.9 slim-the-overlay — code done** (inline settings panel + handlers removed from `index.html`/`main.ts`; the mute-others toggle migrated into the Settings window; overlay is now orb + card + inject) — needs Mac verify. **Remaining in M4: only the 4.10 exit demo** (on-Mac, across a live vendor) + the on-Mac verification of 4.2/4.3/4.7/4.8/4.9. Full per-phase checklist + "Progress at a glance" in `docs/product/m4-tasks.md`.
- **M5 Quality & polish (daily-driver)** — **IN PROGRESS (started 13 Aug 2026).** A parallel settings-plan effort (`docs/product/settings/`, its own phases 1–7) delivered a slice of M5 early; `m5-tasks.md` was re-verified against the live code on 13 Aug. **Done: 5.2 custom vocabulary** (list store in `lists.rs`; correction-side "Known terms" injection through pyai/openai/anthropic via `vocabularyNote`; Deepgram `keywords`/`keyterm` STT boost; `vocabulary.test.ts`). **Done: 5.6 telemetry scaffolding** (`packages/core/src/telemetry/telemetry.ts` — opt-in, allow-list, content-free `NoopSink`; wired into `server.ts` session_start/finalize/error; Settings toggle; **transport parked**). **Partial: 5.1 reliability** (retry-with-backoff on PyAI+OpenAI correction only — streaming STT reconnect/keepalive + Anthropic/batch retry unbuilt). **Partial: 5.4 undo** (paste-last-result accelerator + last-result state re-inject the *clean* result; "revert to raw" remains). **5.0** telemetry privacy model locked; undo + concurrency decisions still open. **Not started: 5.3 formatting modes, 5.5 concurrency, 5.7 dogfood.** Bonus shipped beyond M5 scope: snippet/text-expansion, fn-key hold (`fnkey.rs`), and the `main.rs`→modules split. **Next build: 5.1 reliability/reconnect.** Full re-verified checklist in `docs/product/m5-tasks.md`. **Build session (13 Aug 2026):** implemented the rest of the M5 code — 5.1 reliability (net/retry helper; Anthropic+batch retry; Deepgram KeepAlive; `startReconnectingSession` live wrapper wired into the backend; transient/terminal error kinds), 5.2 residual (OpenAI STT keyword hint), 5.3 formatting modes (prose|message|code|raw — core+backend+Rust config+Settings dropdown), 5.4 revert-to-raw (LAST_RAW + commands + configurable accelerator + Settings capture), 5.5 concurrency guard, 5.6 latency capture into `session_finalize`, and the 5.0/5.7 docs. 184 core tests green + typecheck clean (Linux harness). **Remaining:** on-Mac `cargo build`/`npm run widget` verify of the Rust (5.3/5.4) + live reconnect, the 5.6 perf pass (F9/F10), and the 5.7 two-week dogfood.
- **Meetings / "Granola mode" (N-track)** — **DEMO PROTOTYPE LANDED (13 Aug 2026, commits `012d11f` + `cf993e2`).** Plan of record: **`docs/product/meetings-plan.md`** (north star, N0 gate, milestones N1–N5, decisions, and **§13 Build log** with the honest verified/unverified split). This is scaffolding built ahead of the N0 gate for a hackathon demo — **a working demo is not a cleared gate.**
  - **Built + tested (37 tests, typecheck clean):** `packages/core/src/meetings/` — `transcript.ts` (two capture streams → one append-only, timestamped, speaker-tagged timeline; deliberately NOT `TranscriptAccumulator`, which collapses repeats and stitches revisions of a *single* speaker), `stereo.ts` (mic→L / system→R interleave, pads the short side with silence rather than truncating), `pyai.jobs.ts` (async `/v1/transcription/jobs` with `channel:true`), `prompt.ts` (6 templates), `openai.summarize.ts` + `registry.ts` behind a `MeetingSummarizer` interface.
  - **Built, NEVER RUN:** `apps/backend/src/meeting.ts` (separate WS server on **:8788**) and `apps/widget/meetings.{html,ts,css}` (dual-stream webview capture). No end-to-end run has happened — the pieces are tested against mocks only.
  - **Key architectural finding (probe, 13 Aug):** Hear **streaming has no diarization** (`diarize`/`diarization`/`speaker_labels` are silently ignored, no speaker field on any event) and is **still English-only**. But `POST /v1/transcription/jobs` offers **`channel:true` — "exact, model-free speaker separation per channel."** Since a meeting is already two physical captures, muxing mic→L/system→R makes Me/Them attribution *exact* rather than a model's guess, and async Transcribe is cheaper ($0.0005/min vs $0.001/min). Live streaming stays the on-screen preview; the jobs pass is authoritative on stop — the same shape as M2.
  - **Hallucination guard is enforced in CODE, not prompt:** every action item must carry a verbatim transcript quote; `locateQuote()` checks the real transcript and drops anything unanchored (tested with a fabricated quote).
  - **Deliberate isolation:** the meetings backend shares nothing with `server.ts` but `@verbatim/core`. If meetings break, dictation still works. Worth keeping past the demo.
  - **Demo shortcut, deliberately taken:** capture is **webview-only, zero new Rust** — BlackHole makes system audio an ordinary input device so both streams are `getUserMedia`. Core Audio process taps (meetings-plan §2 N0-A) remain the shipping answer; the loopback-driver dependency is exactly the UX cost N0-A exists to avoid.
- **First-run onboarding (O-track)** — **AUTHORED 18 Aug 2026, NOT YET BUILT.** Phases **O1–O7** of `docs/product/onboarding-plan.md` implemented by three parallel agents against `docs/onboarding/implementation-plan.md`; O8 (local models) deliberately untouched. TS typecheck green, resolver truth table green, Rust parses under `rustfmt` only. **No `cargo build`, no `npm run widget`, no macOS runtime anywhere in that pipeline.** Full detail in §First-run onboarding below.

## First-run onboarding (O-track) — authored 18 Aug 2026, **`cargo build` GREEN 30 Aug 2026**

Plan of record: `docs/product/onboarding-plan.md` (design) · `docs/onboarding/implementation-plan.md` (the 24-task build plan) · `docs/onboarding/progress-dev-{a,b,c}.md` (what each agent did) · **`docs/onboarding/review.md` (independent pre-Mac review — read this before the first build)**.

**What the flow now is:** the `onboarding` window (440×**566**, was 480) is a 3-screen flow — **Connect** (paste one key, vendor detected from its shape, editable chip, verified before saving, inline second-role slot when the first key covers only one role), **Two macOS permissions** (in-window mic prompt, AX deep link + 1 Hz self-updating poll, never blocking), **Give it one try** (renders the user's own dictation from a new JS→JS event, so it does not depend on injecting into our own window). Exits go through a new `finish_onboarding` command that hides the window **and reverts the activation policy** — closing the Dock-icon leak — then records `setup_state`, so "Set up later" is remembered instead of re-prompting every launch.

**Landed as code (all authored this pass):**
- **New TS:** `apps/widget/src/onboarding-resolve.ts` (pure, import-free resolver: detection, role gating, the config patch, `sanitizeCorrection`), a rewritten `src/onboarding.ts` / `onboarding.html` / `onboarding.css`.
- **New Rust:** `src-tauri/src/verify.rs` (`key_verify`, `ureq` 2.x, 2s timeout, off the main thread via `#[tauri::command(async)]`; 401/403 ⇒ *rejected*, anything else ⇒ *saved anyway*; `pyai` has no probe yet so it always takes the saved-anyway branch), `src-tauri/src/testkey.rs` (`option_env!("VERBATIM_PYAI_TEST_KEY")`, so a public build cannot carry the internal key; `test_key_available` / `use_test_key` / internal window watermark).
- **Edited Rust:** `config.rs` (`setup_state: String`, default `"unseen"`, in the struct **and** `impl Default`), `window.rs` (`finish_onboarding`, `show_onboarding_window`), `tray.rs` (`build_menu`/`refresh_menu`, conditional **Finish setup…**, and the show item's hotkey label now read from config instead of a hardcoded `⌥Space`), `main.rs` (2 `mod`s, **5 new handler entries — 52 total, no duplicates**, the `setup_state`-aware launch gate, watermark call, header module map).
- **`Cargo.toml` was edited** (it was on the plan's do-not-touch list): `ureq = "2"` moved from the macOS target table to top-level `[dependencies]`, because `mod verify;` is unconditional. `Cargo.lock` unaffected. Judged necessary by the reviewer.
- **Overlay (`src/main.ts`):** emits `dictation-progress` (`live`/`correction`/`final`, fire-and-forget) for the try-it screen, and replaces the raw `Provider 'pyai' needs: PYAI_API_KEY` on a first dictation with *"Verbatim isn't set up yet"* + a **Finish setup** button — but only after positively confirming via `get_config` + `has_key` that no speech source exists, so a configured user never loses a real error (and its Copy-details) to the nudge. Once per launch.
- **Settings (`src/settings.ts`):** `capabilityErrors()` now checks the configured ids against the **registries**, not just the key store — a config carrying `correctionProvider: "pyai"` reported **zero** errors before (the PyAI key is saved) and now reports an unusable-provider error; a provider needing **no** key counts as satisfied instead of reading "needs undefined". A provider id that isn't in the dropdown renders as a selected, disabled `<id> (unavailable)` option instead of a blank field.
- **New scripts/docs:** `scripts/assert-no-test-key.sh` (release-checklist gate; CI has **no macOS runner**, so it is not a CI job yet) and the five files under `docs/onboarding/`.

**COMPILED AND CLEAN as of 30 Aug 2026.** `cargo build` on the Mac finished with **0 errors** (7.79s, real compile). **None of the predicted failures occurred** — `tray.rs`'s `&dyn IsMenuItem` vec + `Menu::with_items` slice, `tray_by_id("main-tray")`'s `PartialEq<&str>` bound, and `MenuItem::with_id`'s generics all typechecked as written, so the `implementation-plan.md` §8 fallbacks (R6, R9) are **not needed**. Remaining warnings are 3 benign dead-code/unused-import items (`tray.rs:94` `use tauri::Manager`, `axinject.rs:402` `read_focused_field_text`, `fnkey.rs:360` `is_running`). Mac-verify item 1 is therefore **cleared**; items 2–12 (runtime) remain. Historical note follows.

**Previously — authored but never compiled, the entire Rust half.** `verify.rs`, `testkey.rs`, `config.rs`, `window.rs`, `tray.rs`, `main.rs`: `rustfmt` parses them and the `ureq` 2.12.1 + `tauri-macros` 2.6.3 APIs they use were read against the real crate sources, but **nothing typechecked or borrow-checked them**. Likeliest residual failures, in order: `tray.rs`'s `&dyn IsMenuItem` vec + `Menu::with_items` slice, `tray_by_id("main-tray")`'s `PartialEq<&str>` bound, and `MenuItem::with_id`'s text/accelerator generics. Fallbacks for the two highest-risk constructs are pre-written in `implementation-plan.md` §8 (R6, R9).

**Known open items in the new code (from `docs/onboarding/review.md`; none fixed as of this entry):**
- **major** — a 401 on the **second** key is attributed to the **first** vendor and reddens the first field (`onboarding.ts:488` with `:227`/`:248`); the user is sent to edit the wrong field.
- `scripts/assert-no-test-key.sh` uses `grep -q` where it needs `grep -qF` — a regex metacharacter in the key prefix makes the secret gate **fail open**.
- The "Couldn't reach `<Vendor>` — saved anyway" chip is never painted on the advancing path (no `render()` between the assignment and Screen 2), so an unverified key can look verified.
- `sanitizeCorrection(cfg.correctionProvider ?? "")` treats a failed boot `get_config` as an invalid value and can downgrade a valid stored `anthropic` to `openai`.
- **Copy accuracy:** Screen 1's trust line says keys are *"stored in your macOS keychain"*, but the default secret backend is `key_storage: "local"` — a `0600` `secrets.json` in the app config dir (`secrets.rs:9`); the keychain is the non-default branch. Either the copy or the default needs to change before public release.
- **✅ FIXED 30 Aug 2026 (branch `mayank`).** Pre-existing, NOT from this pipeline: `lists.rs`'s `glossary_get` / `glossary_save` were `#[tauri::command]` but missing from `invoke_handler`, while `settings.ts` invokes them — the Settings "Names & Jargon" section fails at runtime. Entry arithmetic (47 + 5 = 52) proves this pipeline did not drop them.

**Mac-verify list (nothing below was run anywhere in the pipeline).** Ordered as in `review.md` §5; the numbered M-items of `implementation-plan.md` §9 are the fuller checklist.
1. `cargo build` in `apps/widget/src-tauri` — the one gate that could not be approximated.
2. The Dock-icon leak is really gone: with `dockIcon: false`, finish onboarding through **each** of the three exits and watch the Dock; repeat with `dockIcon: true` and confirm the icon is **kept**.
3. `key_verify` against live vendors: one real 401 per vendor, plus a Wi-Fi-off run that must say *saved anyway*, never *rejected*.
4. `setup_state` round trip on a **pre-existing** `settings.json` that lacks `setupState` — it must parse and gain the default.
5. `use_test_key` / `test_key_available` on both build variants, then `bash scripts/assert-no-test-key.sh <app>` on a clean build **and** on a deliberately poisoned one (fix `grep -qF` first).
6. Tray: `refresh_menu` actually swaps the live menu and **Finish setup…** disappears once `setup_state == "done"`.
7. Screen 2's real prompts: `getUserMedia` asking once, `ax_trusted` flipping live (design-doc §9 #2 — if a relaunch turns out to be required, Screen 2 needs a **Relaunch Verbatim** button), and `alwaysOnTop` vs System Settings.
8. Screen 3 end to end: `dictation-progress` crossing the window boundary on a real hotkey hold; note separately whether injection *also* landed in the window's own field (closes design-doc §9 #3).
9. Visual pass at 440×566 against `docs/product/onboarding-prototype.html`, including the worst-case Screen 1 (Anthropic first + required slot + error line), and `onboarding.css`'s unscoped generic classes inheriting from `settings.css`.
10. Regression: the overlay (dictation, correction reveal, injection, banners, Show Last Result, command mode, history) and Settings (all four vendor rows, both provider selects, capability errors for every combination **including** a `correctionProvider: "pyai"` config).
11. **Rotate the PyAI key** pasted during early development before anything from O6 ships (still open, see below).
12. PyAI probes that only the Mac can run: key prefix + cheapest authenticated GET (design-doc §9 #1) — until then `key_verify("pyai")` deliberately never verifies.

## Tests (all green in cloud)
- 77 unit/integration tests (`npm test`) — pipeline, accumulator (16 in-depth streaming cases), reconstruct, wav/pcmToWav, segmenter, the core **settings/capability** layer (12), the multilingual **prompt language-note** (3), and vendor adapters vs mock servers: PyAI, **Deepgram STT (3)**, **OpenAI STT+correction**, **Anthropic correction**.
- **+37 meetings tests** (`packages/core/src/meetings/{meetings,jobs}.test.ts`) — transcript merge/dedupe, speaker labels, quote anchoring (incl. the fabricated-quote guard), templates, the OpenAI summarizer vs mocks, stereo interleave, and the PyAI jobs lifecycle (poll / `result_url` / failure / timeout).
- 1 Playwright e2e (`npm run test:e2e`) — headless-Chromium demo flow (stream → diff → formatted, no console errors).

## How to run
```bash
cd open-dictation && npm install
npm run dev            # backend + web → http://localhost:5173 ; click "Demo (no mic)"
# live: put PYAI_API_KEY in .env at repo root, then npm run dev → "Start dictation"
npm test               # unit
npm run test:e2e       # e2e (dev: npx playwright install chromium first)
# widget spike (Mac): cd apps/widget && npx tauri dev

# ── Meetings (N-track prototype) ──
npm run meetings       # meetings backend on :8788 (dictation backend stays on :8787)
npm run widget         # then: sidebar → Meetings   (or http://localhost:5175/meetings.html in Chrome)
# Needs PYAI_API_KEY + OPENAI_API_KEY, and a macOS Multi-Output Device
# (headphones + BlackHole) as system output. PRESS "Test levels" FIRST —
# if both meters don't move, nothing downstream will work.
# Output → ~/Documents/Verbatim/Meetings/<timestamp>-<slug>/{note.md,session.json}
```

## Immediate next steps (pick up here)

**Before anything else (18 Aug 2026):** `cd apps/widget/src-tauri && cargo build`. Six `.rs` files from the onboarding pipeline have never been compiled (§First-run onboarding). Read `docs/onboarding/review.md` first — it lists the two fixes worth making *before* the build attempt (the second-key 401 attribution, and `grep -qF` in `scripts/assert-no-test-key.sh`) and the pre-written fallbacks for the two riskiest Rust constructs.

1. Git is on `master` with a **private remote** now wired (`origin` → `github.com:verbatimai/verbatim`, SSH) and merged. `master` has no upstream tracking set — `git branch --set-upstream-to=origin/master master` to simplify future pulls. **Note:** network + SSH live on your Mac, not in a cloud Cowork session, so `git fetch`/`pull` must be run from your own terminal; and committing *through* a cloud session leaves stray `.git/*.lock` files **and orphaned `.git/objects/tmp_obj_*`** (the bridge can't unlink anything inside `.git`) — run git locally to avoid that. **Cleanup after any cloud-session commit:** `find .git/objects -name 'tmp_obj_*' -delete && rm -rf _to_delete_git_locks _stray_to_delete _to_delete && git fsck`.
2. Wire `npm run test:e2e` into `.github/workflows/ci.yml`; add widget CI (`cargo build` on a macOS runner + `npm run typecheck`).
3. **M4 is in progress** (see the M4 line above + `m4-tasks.md`). **All four vendor adapters and the settings UI are now done** (Deepgram 4.4 landed + 3 tests green in cloud; 4.7's settings UI + multilingual landed alongside it). **Next up:** (a) **on-Mac interactive verification of 4.2 + 4.3 + 4.7** — they `cargo build`/`vite build` clean and the release `.app` launches, but haven't been click-tested: open Settings, type a key, capture a real hotkey combo, confirm the overlay still injects while Settings is open and closing it doesn't quit; confirm `get_config`/`set_config` round-trip + persist and a keychain key survives restart. Then (b) **verify 4.8's dev sidecar wiring** — `cargo build` + `npm run widget` (Rust now spawns the backend itself; grep the `start` frame to confirm no key crosses the renderer; switch vendors without relaunch; quit → no orphan on `:8787`) and build the **release sidecar packaging** (compile+sign the backend binary → `externalBin` via `tauri-plugin-shell`; the release spawn branch is currently a TODO). Then (c) **slim the overlay (4.9)** and (d) **exit demo (4.10)**. **Rust note:** all `src-tauri` changes must be `cargo build`/`npm run widget`-verified on the Mac (can't compile in the cloud authoring env).
4. **Meetings / N-track — smoke test first, then decide.** (a) `npm run meetings` + `npm run widget` → Meetings → **Test levels**; both meters must move. (b) Record a 2-minute call end to end and confirm `~/Documents/Verbatim/Meetings/<...>/note.md` appears. (c) **A/B `PYAI_JOBS_MODEL=pyai-hear` against the `pyai-hear-telephony` default** on real wideband audio — this decides whether speaker separation is exact or falls back. (d) Then read `meetings-plan.md` §11 for the three still-open decisions (track naming, M5-vs-N1 priority, positioning) and §2 for the N0 gate that this prototype does **not** clear.

## Parallel-session notes (read before starting work)

**Ports.** `:8787` dictation backend (`server.ts`) · `:8788` meetings backend (`meeting.ts`) · `:5175` widget Vite dev server. The two backends are independent processes and share nothing but `@verbatim/core`.

**Lanes — pick one, they don't collide:**

| Lane | Files | Notes |
|---|---|---|
| Dictation / M5 | `apps/backend/src/server.ts`, `packages/core/{pipeline,correction,providers}`, overlay `src/main.ts` | The shipping product. Don't destabilise it for meetings work. |
| Meetings / N-track | `packages/core/src/meetings/*`, `apps/backend/src/meeting.ts`, `apps/widget/meetings.*` | Fully isolated by design. |
| Settings waves | `apps/widget/settings.{html,ts}`, `src-tauri/src/config.rs` | See `docs/product/settings-plan.md`. |
| Rust / native | `apps/widget/src-tauri/src/*` | **Mac only** (below). |

**Known collision hot spots:**
- **`AppConfig` (`src-tauri/src/config.rs`) is THE shared file.** The settings plan adds ~11 fields, meetings will add ~6 (meeting vendor, retention, auto-start, tap allowlist, storage path). `set_config` deserializes the *whole* merged object, so **every new field needs `#[serde(default)]` coverage AND a `Default` entry**, or an existing `settings.json` fails to parse. Land schema fields for both tracks in one commit up front rather than racing.
- `packages/core/src/index.ts` re-exports with `export *`, so **new exports can collide silently**. `correction/prompt.ts` already owns `SYSTEM_PROMPT` / `userMessage` — the meetings module had to rename to `MEETING_SYSTEM_PROMPT` / `meetingUserMessage`. Grep before adding a top-level export.
- `main.rs` was split into 12 modules (13 Aug) precisely so parallel tracks stop fighting over it — add new commands in their own module + one `generate_handler!` line.

**Hard rules:**
- **Rust cannot be compiled in a cloud session.** Anything under `src-tauri/` must be `cargo build` / `npm run widget`-verified on the Mac. `rustfmt` is a usable *syntax* gate in the cloud, nothing more.
- **Network:** cloud sessions cannot reach `api.pyai.com` (proxy 403) and the device bridge has no network at all. **PyAI probes must be run from the Mac terminal** — `experiments/scripts/probe_hear_caps.py` is the current one.
- **Secrets:** `.env` holds a live key. It must never enter this project, a chat, or a commit. The leaked PyAI test key still needs rotating before M6.

## Known open items / caveats
- **Live long-input transcript** can still show occasional stitch artifacts (Hear streams overlapping/revising windows). The final batch output is unaffected. To improve: capture the raw stream with `PYAI_STT_DEBUG=1 npm run backend`, save a few `[hear]` lines, and tune `mergeOverlap` / add a fixture in `packages/core/src/pipeline.test.ts`.
- **F9** — real correction/format latency on live PyAI not yet measured.
- **F10 — ✅ ANSWERED (13 Aug 2026, openapi probe).** The finalize control message is **`{"type":"commit"}`** (the literal text frame `EOF` is an equivalent flush; closing the socket also flushes). `endpointing_ms` is configurable at connect or mid-session. Also: **`protocol=pyai-hear-v1`** selects the published frame names — omitting it keeps the legacy `fusion-v0` names (`transcript.partial`, `session.created`), which is what the current adapter reads. **Close F10 in `docs/research/pyai-api-findings.md` and consider adopting `commit`.**
- **PyAI 404 outage — resolved.** The M4 line above notes PyAI externally 404-ing; a live probe on 13 Aug (late) got clean 200s on `/v1/audio/transcriptions` and a working streaming WS. Treat that note as stale.
- **Meetings caveats (all unverified):** nothing has run end to end; PyAI jobs `channel:true` has never been exercised on real wideband stereo (**the jobs API defaults to `pyai-hear-telephony`, tuned for 8 kHz — A/B against `pyai-hear` via `PYAI_JOBS_MODEL` before relying on it**); `ScriptProcessorNode` is deprecated and should become an AudioWorklet; clock drift between two independent `AudioContext`s is unmeasured (set BlackHole and the mic to the same sample rate); and **without headphones the mic re-records the far end, which visibly breaks channel separation.**
- **Security:** rotate the PyAI test key that was pasted during development before going public. This also gates O6's internal test-key button — `scripts/assert-no-test-key.sh` must be run (and its `grep -qF` fix applied) before any artifact leaves the building.
- **✅ FIXED 30 Aug 2026** — added `lists::glossary_get` + `lists::glossary_save` to `main.rs`'s `invoke_handler` (53 → 55 entries); the 7 `lists.rs` dead-code warnings disappeared on rebuild, confirming registration. Original report: pre-existing Settings bug, found during the onboarding review (not caused by it): `lists.rs`'s `glossary_get` / `glossary_save` are declared `#[tauri::command]` but are missing from `main.rs`'s `invoke_handler`, while `settings.ts:977`/`:984` invoke them — the "Names & Jargon" section silently fails to load or save. One line each in the handler list.

## Map
- `packages/core` — vendor-neutral pipeline (providers, correction, accumulator, batch, diff) **+ `meetings/` (N-track: transcript model, templates, summarizer registry, stereo mux, PyAI jobs adapter)**.
- `apps/backend` — dev WS bridge (live preview + batch-on-stop). `apps/web` — Vite demo UI. `apps/widget` — Tauri macOS widget (M3) **+ `meetings.html` (N-track recorder UI)**. `apps/backend/src/meeting.ts` — **separate** meetings WS server on `:8788`.
- `docs/product` — plan, roadmap, m3/m4/m5-tasks, **`meetings-plan.md` (N-track plan of record)**, settings-plan, this status. `docs/architecture` — overview, vendor-apis, macos-injection, tauri-stack, multilingual, git-and-release. `docs/research` — PyAI findings (internal). `experiments/` — throwaway probes (internal).
