# Nemotron ASR weights (~600 MB) — stored with Git LFS so clones get the model
# without Hugging Face credentials. After clone: git lfs pull
models/nemotron/*.gguf filter=lfs diff=lfs merge=lfs -text
