{
  "name": "@verbatim/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "M2 live web demo: speak -> live transcript + visible correction.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
