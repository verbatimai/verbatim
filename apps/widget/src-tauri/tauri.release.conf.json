{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeBuildCommand": "npm run build && node ../../scripts/build-sidecar.mjs"
  },
  "bundle": {
    "externalBin": ["binaries/verbatim-backend"],
    "macOS": {
      "entitlements": "entitlements.plist",
      "minimumSystemVersion": "11.0"
    }
  }
}
