{
  "name": "@verbatim/widget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "macOS dictation widget (Tauri). M3: non-activating overlay + M2 pipeline UI + focused-field injection.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "tauri": "tauri",
    "start": "tauri dev"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}